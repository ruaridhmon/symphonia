/**
 * App.tsx — kept as a re-export of Dashboard for backwards compatibility.
 * The layout shell (Header + Footer) is now provided by PageLayout via the router.
 */
export { default } from './Dashboard';
